class ComputeColorScrolling<float2 Speed> : ComputeColor, Texturing
{
    compose ComputeColor baseColor;

    override float4 Compute()
    {
        streams.TexCoord = streams.TexCoord + Speed * Global.Time;
        return baseColor.Compute();
    }
};